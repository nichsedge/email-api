# Database Configuration
DATABASE_URL=sqlite:///./email_api.db
# For production: DATABASE_URL=postgresql://user:password@localhost/email_api

# Security Configuration
SECRET_KEY=your-super-secret-key-here-change-this-in-production
ENCRYPTION_KEY=your-encryption-key-here-generate-with-openssl

# Email Configuration (Default - for backward compatibility)
GMAIL_EMAIL=your-default@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# Environment Configuration
ENVIRONMENT=development  # development or production

# CORS Configuration (for production)
CORS_ORIGINS=["https://yourdomain.com", "https://www.yourdomain.com"]

# Rate Limiting Configuration
DEFAULT_RATE_LIMIT_PER_MINUTE=60
DEFAULT_RATE_LIMIT_PER_HOUR=1000

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# API Key Configuration
DEFAULT_API_KEY_SCOPES=["read"]
DEFAULT_RATE_LIMIT_PER_MINUTE=60
DEFAULT_RATE_LIMIT_PER_HOUR=1000